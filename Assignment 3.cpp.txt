#include<iostream>

using namespace std;

int main() {
	int arr1[10], arr2[10], arr3[10], sum = 0, n;
	cout << "Type size of arrays: "; cin >> n;
	for (int i = 0;i < n;i++) {
		cout << "Enter elements of 1st array [" << i << "]: "; cin >> arr1[i];
	}cout << endl;
	for (int i = 0;i < n;i++) {
		cout << "Enter elements of 2nd array [" << i << "]: "; cin >> arr2[i];
	}
	for (int i = 0;i < n;i++) {
		arr3[i] = arr1[i] + arr2[i];
		cout << arr3[i];
	}

	return 0;
}